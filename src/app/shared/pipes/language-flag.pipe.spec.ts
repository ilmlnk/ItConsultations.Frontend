import { LanguageFlagPipe } from './language-flag.pipe';

describe('LanguageFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
