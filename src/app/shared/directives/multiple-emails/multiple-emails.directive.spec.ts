import { MultipleEmailsDirective } from './multiple-emails.directive';

describe('MultipleEmailsDirective', () => {
  it('should create an instance', () => {
    const directive = new MultipleEmailsDirective();
    expect(directive).toBeTruthy();
  });
});
